import AppKit

class AltKeyMonitor {
    static var isAltDown: Bool = false
    static var onAltChange: ((Bool) -> Void)?

    static func start() {
        // Placeholder — wired in later phases.
    }
}
